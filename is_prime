#prime_numbers
import time
def prime_id(x):
    prime = True
    for i in range(2,x):
        if x % i == 0:
            prime = False
            break
    return prime

#print(prime_id())
start = time.perf_counter()
for i in range(1,100001):
    prime_id(i)
end = time.perf_counter()
print(end - start)
